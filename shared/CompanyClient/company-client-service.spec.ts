import { CompanyClientService } from './company-client-service';

describe('CompanyClientService', () => {
  it('should create an instance', () => {
    expect(new CompanyClientService()).toBeTruthy();
  });
});
